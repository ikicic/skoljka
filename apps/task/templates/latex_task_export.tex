\documentclass[10pt,a4paper,oneside,final]{article}

\usepackage[margin=2cm]{geometry}

\usepackage{graphicx}

\usepackage{cmap}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage[croatian]{babel}
\usepackage[centertags,intlimits,namelimits,sumlimits]{amsmath}
\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage{enumitem}
\usepackage[normalem]{ulem}

\usepackage[HTML]{xcolor}
\definecolor{btn_primary}{HTML}{0055CC}
\definecolor{css_gray}{HTML}{808080}

\usepackage{fancyhdr}
\fancypagestyle{empty}{
    \fancyhf{}
    \renewcommand{\headrulewidth}{0pt}
    \renewcommand{\footrulewidth}{0pt}
}
\fancypagestyle{plain}{
    \fancyhf{}
    \fancyfoot[R]{\footnotesize\bf\thepage}
    \renewcommand{\headrulewidth}{0pt}
    \renewcommand{\footrulewidth}{0pt}
    \renewcommand{\footrule}{\vskip-\footrulewidth \hrule width\headwidth height\footrulewidth}
}

\usepackage{hyperref}
\hypersetup{
    unicode=true,
    colorlinks=true,
    pdfborder={0 0 0},
    linkcolor=btn_primary,
    citecolor=btn_primary,
    filecolor=btn_primary,
    urlcolor=btn_primary,
    pdfstartpage=1,
    pdfstartview=FitH,
    pdfnewwindow=true
}

\setlength{\parindent}{0pt}
\setlength{\parskip}{6pt}

\DeclareMathOperator{\tg}{tg}
\DeclareMathOperator{\ctg}{ctg}

\pagestyle{plain}

\begin{document}

{% for data in tasks %}
    {% if has_title %}
        \subsection*{
            \color{btn_primary}
            {{ data.title }}
        }
    {% endif %}
    {% if has_url or has_source %}
        \begin{flushright}
            {% if has_url %}
                \url{ http://www.skoljka.org{{ data.url }} }
            {% endif %}
            {% if has_source and data.source %}
                \textbf{Izvor: }
                {{ data.source }}
            {% endif %}
        \end{flushright}
    {% endif %}
    {% if has_index %}
        {{ data.index }}.
    {% endif %}
    {% if has_id %}
        ({{ data.id }})
    {% endif %}
    {{ data.content }}
{% endfor %}

\end{document}

